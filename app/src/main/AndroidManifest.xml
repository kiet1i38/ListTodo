<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE"/>
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>
    <uses-permission android:name="android.permission.VIBRATE"/>
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM"/>
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ListTodo"
        android:usesCleartextTraffic="true"
        tools:targetApi="31">

        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyDLklT-3sObA8pGfU5Oy0409_iT6JaktDo" />

        <activity
            android:name=".activities.LoginActivity"
            android:exported="true"
            android:windowSoftInputMode="adjustPan">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".activities.MainActivity" android:exported="false"/>
        <activity android:name=".activities.RegisterActivity" android:exported="false"/>
        <activity android:name=".activities.ForgotPasswordActivity"/>

        <activity android:name=".activities.AddTaskActivity" />
        <activity android:name=".activities.EditTaskActivity"/>
        <activity android:name=".activities.EditSubtaskActivity" />

        <activity android:name=".activities.TimerActivity"/>
        <activity android:name=".activities.AddTimerActivity"/> 
        <activity android:name=".activities.TimerRunningActivity"/>

        <activity android:name=".activities.CountdownActivity"/>
        <activity android:name=".activities.AddCountdownActivity"/>

        <activity android:name=".activities.LocationActivity"/>
        <activity android:name=".activities.StatsActivity"/>
        <activity android:name=".activities.DateCalcActivity"/>
        <service android:name=".services.AlarmSoundService" />

        <service android:name=".services.TimerService" android:foregroundServiceType="shortService" />
        <receiver android:name=".receivers.AlarmReceiver"/>

    </application>

</manifest>
