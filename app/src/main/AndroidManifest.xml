<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <meta-data
        android:name="com.google.android.geo.API_KEY"
        android:value="YOUR_API_KEY_HERE" />
    <!-- Em phải lên Google Cloud Console tạo Key free và dán vào đây -->

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE"/>
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/> <!-- Android 13+ -->
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM"/>

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ListTodo">
        <activity android:name=".activities.TimerActivity"/>
        <activity android:name=".activities.CountdownActivity"/>
        <activity android:name=".activities.LocationActivity"/>
        <activity android:name=".activities.EditTaskActivity"/>
        <activity android:name=".activities.StatsActivity"/>
        <activity android:name=".activities.DateCalcActivity"/>

        <!-- Khai báo Service (BẮT BUỘC) -->
        <service android:name=".services.TimerService" android:foregroundServiceType="shortService" />

        <!-- Khai báo Receiver (BẮT BUỘC) -->
        <receiver android:name=".receivers.AlarmReceiver"/>
        <activity
            android:name=".activities.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>


        <activity android:name=".activities.AddTaskActivity" />

    </application>

</manifest>