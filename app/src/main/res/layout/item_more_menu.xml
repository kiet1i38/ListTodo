<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="60dp"
    android:gravity="center_vertical"
    android:paddingHorizontal="16dp"
    android:background="?attr/selectableItemBackground">

    <ImageView android:id="@+id/img_icon" android:src="@drawable/ic_dashboard" android:layout_width="24dp" android:layout_height="24dp" android:tint="@color/blue_primary"/>
    <TextView android:id="@+id/tv_title" android:text="Tên chức năng" android:textSize="16sp" android:layout_marginStart="16dp" android:layout_weight="1" android:layout_width="0dp" android:layout_height="wrap_content"/>
    <ImageView android:src="@drawable/ic_menu" android:rotation="180" android:layout_width="16dp" android:layout_height="16dp" android:tint="@color/text_gray"/> <!-- Mũi tên -->
</LinearLayout>